<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"

       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       https://www.springframework.org/schema/beans/spring-beans.xsd

       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd

       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd">

<import resource="spring-hibernate.xml"/>

<!-- application.properties ko load karke uski properties Spring ko available karata hai -->
<context:property-placeholder location="classpath:application.properties"/>


    <context:component-scan base-package="com.exam"/> 
    <!-- Spring starts
			↓
	com.exam.controller package me jata hai
			↓
	Saari classes scan karta hai
			↓
	Jis class par @Controller milta hai
			↓
	Us class ka object banata hai
			↓
	Us object ko Spring Container me rakh deta hai
	
	(Yani sirf identify nahi karta.
	 Instantiate bhi karta hai.)
	 
	<context:component-scan> Spring ko batata hai ki
	 kis package ko scan karna hai. 
	 Scan karne ke baad Spring @Controller, @Service, @Repository, @Component
	  wali classes ke objects banakar unhe Spring Container me store kar deta hai.
	 
	 
	 """" component-scan package ko scan karta hai, 
	 aur annotated classes (@Controller, @Service, @Repository, @Component)
	  ke objects bana kar Spring IoC Container me store kar deta hai.""""
	  
	 """IoC Container objects ko create bhi karta hai,
	  manage bhi karta hai aur store bhi karta hai."""
	
	IoC ka matlab hai object banane aur manage karne ka control programmer se 
	Spring IoC Container ke paas chala jana.
	 -->

    <mvc:annotation-driven/>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">

        <property name="prefix" value="/WEB-INF/views/"/>

        <property name="suffix" value=".jsp"/>

    </bean>
    
    <mvc:interceptors>
    <mvc:interceptor>
    <mvc:mapping path="/admin/**"/>
    <mvc:mapping path="/student/**"/>
    <bean class="com.exam.interceptor.LoginInterceptor"/>
                 
    </mvc:interceptor>
    </mvc:interceptors>
    
    <!-- Yeh </beans> se pehle add kiya hai multipart resolver hai image ke loye -->
	<bean id="multipartResolver"
	    class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
	    <property name="maxUploadSize" value="5242880"/>
	</bean>
	
	<!--
Browser ke /profile-images/** URL ko
external profile image folder se connect karta hai.

${profile.upload.path} ki value application.properties se aayegi.
-->
<mvc:resources
    mapping="/profile-images/**"
    location="file:///${profile.upload.path}" />

<!-- Naya shared CSS/JS (professional design system) serve karne ke liye -->
<mvc:resources
    mapping="/assets/**"
    location="/assets/" />

</beans>